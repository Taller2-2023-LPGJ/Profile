build:
  t2-profile:
    context: .
    dockerfile: Dockerfile

  t2-profile-prisma:
    context: .
    dockerfile: Dockerfile

  t2-profile-db:
    context: .
    image: postgres:latest

deploy:
  compose:
    - file: docker-compose.yaml