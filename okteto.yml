name: Profile

dev:
  api:
    command: ["bash"]
    forward:
      - 3001:3001
    sync:
      - api:/usr/src/app
  t2-profile:
    sync:
      - t2-profile:/usr/src/app
  
deploy:
  compose:
    file: docker-compose.yaml